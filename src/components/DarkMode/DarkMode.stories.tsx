import React, { Fragment } from 'react';
import { DarkMode } from '.';

export default { title: 'Components|DarkMode' };

export const defaultDarkMode = () => (
  <Fragment>
    <DarkMode />
  </Fragment>
);
